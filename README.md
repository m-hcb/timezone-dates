# timezone-dates

Instructions to run 

Go to the root folder in terminal

composer install

cp .env.example .env

Create your database in MySql server

php artisan migrate

php artisan serve

and that is it..